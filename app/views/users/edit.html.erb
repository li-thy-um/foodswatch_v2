<% provide(:title, "修改个人资料") %>
<h1>修改个人资料</h1>


<div class="row">
  <div class= "span3">
    <%= gravatar_for @user %>
    <a href="http://gravatar.com/emails" target="_blank">修改头像</a>
  </div>
  
  <div class="span6">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %> 
      <div class="text-center"><%= f.submit "保存修改", class: "btn btn-large btn-danger" %></div>
    <% end %>
  </div>
</div>
